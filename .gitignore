.DS_Store
.claude/

# Terraform
infra/.terraform/
infra/bootstrap/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
infra/backend.hcl
infra/terraform.tfvars
infra/terraform.tfvars.json
infra/bootstrap/terraform.tfvars

# Generated large bronze seed data (millions of rows) — regenerate with
# ingestion/generate_seed_data.py, don't commit.
ingestion/seed_data/

# dbt
dbt/target/
dbt/logs/
dbt/dbt_packages/
dbt/.user.yml
*.duckdb

# Python
__pycache__/
*.pyc
.venv/

# Local Airflow instance (airflow/start.sh) — metadata db, logs, generated
# admin password, PID file. Not the DAGs themselves (airflow/dags/ stays).
airflow/home/
